# 贡献说明

请先通过 Issue 描述问题、复现步骤和预期结果。提交不应包含密码、SSH 凭据、实际管理端点、私人路径、玩家名或管理日志。报安全问题请参考 `SECURITY.md`。

页面改动应优先复用共享组件，保留现有路由和键盘/触屏操作。视觉建议请提供桌面与窄屏对比；不要将未确认的 Minecraft 内容填成正式信息。

提交前运行 README 中的验证命令。状态逻辑应覆盖请求超时、错误响应、过期、恢复以及旧版 API 兼容；后端测试不得依赖真实服务器。Linux 部署脚本测试通过 `python -m unittest discover -s tests -p 'test_deploy_scripts.py'` 运行。

自有代码贡献按本项目 GPL-3.0-only 许可证提交；第三方素材或代码必须注明来源与授权。不要仅依据网络可下载性判断是否可以再分发。
