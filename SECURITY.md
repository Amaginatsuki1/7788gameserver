# 安全反馈

不要在公开 Issue、PR、截图或日志中发布密码、私钥、认证令牌和玩家信息。

若仓库已启用 GitHub 私密漏洞报告，请使用仓库 Security 页面中的 **Report a vulnerability**。若入口不可用，请先建立一个不含漏洞细节和敏感值的 Issue，请维护者提供私密渠道；收到渠道前不要上传敏感内容。

当前只维护最新主分支。生产使用静态导出与独立只读监控服务；开发服务器不应对公网暴露。部署前应检查依赖安全公告、仅公开预期路径，并使用专用受限 SSH 账号。

`npm run check:public` 是规则检查，不保证发现所有秘密，也不自动检查历史提交、远端附件或截图内文字。若密钥曾泄露，应先撤销或轮换，再清理相关文件与历史。
