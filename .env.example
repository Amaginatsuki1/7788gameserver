# Public values embedded in the browser bundle at build time. No secrets here.
# Set these to your own endpoints before deploying a fork.
NEXT_PUBLIC_SITE_URL=https://example.com
NEXT_PUBLIC_STATUS_API_URL=https://example.com/api/status
NEXT_PUBLIC_GAME_PROBE_URL=https://game.example.com/ping
